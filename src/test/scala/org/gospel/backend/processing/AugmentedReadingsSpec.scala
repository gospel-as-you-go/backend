package org.gospel.backend.processing

import org.mockito.integrations.scalatest.MockitoFixture
import org.scalatest.{FlatSpec, FunSuite}

class AugmentedReadingsSpec extends FlatSpec with MockitoFixture {
    it should "augment received readings when everything is ok" in {
        // Arrange
        // Act
        // Assert
    }
}
